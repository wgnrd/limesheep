export class Wurst {
    // Zutaten und Anzahl in Listen speichern!
    public zt: number[] = [0, 0, 0, 0, 0, 0, 0];
    public gesamt = 0;

    constructor(public name: string, public antzt: number []) {
    }
}
